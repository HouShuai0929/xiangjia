<authorization>
  <view class="building">
    <van-cell-group border="{{false}}">
      <van-cell wx:for="{{size}}" wx:key="*this" title="{{point}}{{item+1}}{{type}}" mark:building="{{item+1}}{{type}}" bind:tap="goRoom" is-link />
    </van-cell-group>
  </view>
</authorization>